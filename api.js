const api = {
  baseUrl: '',
  format: 'default', //xml, json,
  auth: {
    username: 'username',
    password: 'password'
  },
  headers: {
    Authorization: 'Bearer ADSDADSADS'
  }
}

export default config;
